<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="na.brexitsoundboard">

    <!-- firebase -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BorisActivity"
            android:label="@string/title_activity_boris" />
        <activity
            android:name=".LiamActivity"
            android:label="@string/title_activity_liam" />
        <activity
            android:name=".DavidActivity"
            android:label="@string/title_activity_david" />
        <activity
            android:name=".TheresaActivity"
            android:label="@string/title_activity_theresa" />
    </application>

</manifest>